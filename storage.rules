rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // DANGER: Completely public rules for debugging.
      // This allows anyone to read, write, and delete any file.
      // This should not be used in production.
      allow read, write: if true;
    }
  }
}
