
{
  "entities": {
    "Genre": {
      "title": "Genre",
      "type": "object",
      "description": "Represents a genre of hymns.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the genre (e.g., Holy Week, Kiahk, Resurrection)."
        },
        "description": {
            "type": "string",
            "description": "A short description of the genre."
        },
        "icon": {
            "type": "string",
            "description": "The name of a lucide-react icon."
        }
      },
      "required": [
        "name",
        "description",
        "icon"
      ]
    },
    "Hymn": {
      "title": "Hymn",
      "type": "object",
      "description": "Represents a hymn within a specific genre.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The title of the hymn (e.g., Tai Shouri, Agios)."
        },
        "genreId": {
          "type": "string",
          "description": "Reference to Genre. (Relationship: Genre 1:N Hymn)"
        }
      },
      "required": [
        "name",
        "genreId"
      ]
    },
    "Recording": {
      "title": "Recording",
      "type": "object",
      "description": "Represents a specific recording of a hymn by a particular cantor.",
      "properties": {
        "hymnId": {
          "type": "string",
          "description": "Reference to Hymn. (Relationship: Hymn 1:N Recording)"
        },
        "cantorId": {
            "type": "string",
            "description": "Reference to Cantor. (Relationship: Cantor 1:N Recording)"
        },
        "audioUrl": {
          "type": "string",
          "description": "URL to the audio file stored in Firebase Storage.",
          "format": "uri"
        },
        "marks": {
            "type": "array",
            "items": {
                "type": "number"
            },
            "description": "An array of timestamps (in seconds) for section marks."
        }
      },
      "required": [
        "hymnId",
        "cantorId",
        "audioUrl",
        "marks"
      ]
    },
    "Cantor": {
      "title": "Cantor",
      "type": "object",
      "description": "Represents a cantor who has recorded hymns.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the cantor (e.g., Cantor Tharwat, Cantor Ibrahim, Cantor Gad)."
        },
        "rank": {
          "type": "number",
          "description": "The display order for the cantor, smaller numbers are higher."
        }
      },
      "required": [
        "name",
        "rank"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/genres/{genreId}": {
      "schema": {
        "$ref": "#/entities/Genre"
      },
      "description": "Stores hymn genres (e.g., Holy Week, Kiahk, Resurrection)."
    },
    "/hymns/{hymnId}": {
      "schema": {
        "$ref": "#/entities/Hymn"
      },
      "description": "Stores hymns, with a reference to their genre."
    },
    "/recordings/{recordingId}": {
        "schema": {
            "$ref": "#/entities/Recording"
        },
        "description": "Stores all recordings for all hymns."
    },
    "/cantors/{cantorId}": {
        "schema": {
            "$ref": "#/entities/Cantor"
        },
        "description": "Stores the list of cantors."
    }
  }
}
